extends layouts/main.pug

block content 
    #newquoteform
        h2 Upload your pdf
        form#uploadForm(ref='uploadForm', action='/pdfupload', method='post', enctype='multipart/form-data')
            input(type='file', name='PDF').newquoteinput
            input(type='submit', value='Upload!').newquoteinput
